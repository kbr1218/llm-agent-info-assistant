# respond_prompt.yaml
template: |
  사용자가 다음과 같이 메시지을 보냈습니다.
  사용자의 메시지: {query}

  {% if context %}
  다음 참고 정보를 바탕으로 사용자에게 자연스럽고 친절하게 답변하세요:
  {context}
  {% else %}
  사용자의 입력에 한국어로 자연스럽고 친절하게 대답하세요.
  {% endif %}
